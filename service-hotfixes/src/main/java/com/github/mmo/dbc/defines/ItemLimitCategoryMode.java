package com.github.mmo.dbc.defines;

public enum ItemLimitCategoryMode {
    ITEM_LIMIT_CATEGORY_MODE_HAVE,                      // limit applied to amount items in inventory/bank
    ITEM_LIMIT_CATEGORY_MODE_EQUIP                             // limit applied to amount equipped items (including used gems)
}
